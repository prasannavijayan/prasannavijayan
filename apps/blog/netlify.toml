# Netlify site base directory = apps/blog (set this in the site's build settings).
[build]
  command = "pnpm run build"
  publish = "dist"
  # Only rebuild the blog when its folder or a shared package changes.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- ':/apps/blog' ':/packages'"
