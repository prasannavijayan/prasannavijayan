# Google Analytics 4 Measurement ID. Shared with the blog site — both domains
# are subdomains of prasannavijayan.in and report into the same GA4
# property/stream. Only fires on the real production host, so leaving this
# unset (or set) locally has no effect on analytics data.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
